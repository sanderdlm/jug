{% extends 'base.twig' %}

{% block main %}
    <h1>News</h1>
    <ul>
        {% for name, path in dir('tests/Fixture/source/news') %}
            {% if name != 'index' %}
            <li><a href="blog/{{ path }}">{{ name }}</a></li>
            {% endif %}
        {% endfor %}
    </ul>
{% endblock %}
